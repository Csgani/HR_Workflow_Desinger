# HR_Workflow_Desinger
This project is a functional prototype of an HR Workflow Designer module built using React and React Flow. It allows HR teams to visually design internal workflows using configurable nodes, connect steps, and test the workflow using a built-in simulation sandbox.
